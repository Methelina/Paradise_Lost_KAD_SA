# AmuleD_v2 — dependency requirements
# Version: 0.1.0
# Updated: 2026-09-22
#
# Groups:
#   Runtime Core   — async networking, crypto, database, REPL, CLI rendering
#   Config         — JSONC/JSON5 parsing
#
# Patch Notes v0.1.0 (Soror L.'.L'.):
#   [+] Initial clean-room requirements for AmuleD_v2 (Python ED2K/Kademlia client).
#   [+] Replaces the legacy borrowed requirements with AmuleD_v2-specific deps.

# === Runtime Core ===
duckdb>=1.5.0
aiohttp>=3.9.0
cryptography>=43.0.0

# === REPL & CLI Rendering ===
prompt_toolkit>=3.0.47
rich>=13.7.0

# === Config (JSONC/JSON5 parsing) ===
json5>=0.9.25
